"""Minimal Python surface so code_context.INDEX yields chunks (functions/classes only)."""


def sample_for_index() -> int:
    """Used by wishlist/08_code_review_context TF–IDF queries."""
    return 42
